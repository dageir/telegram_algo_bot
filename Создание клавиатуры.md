# Кнопки и Клавиатуры

> **Цель:** Научить бота понимать нажатия кнопок, а не только текст.

---

## 1️⃣ Два вида кнопок

В Telegram есть два типа клавиатур. Представь разницу так:

|Тип|Где находится?|Что отправляет?|Для чего лучше?|
|---|---|---|---|
|**Обычная** (`Reply`)|Внизу, вместо клавиатуры телефона|Текст кнопки (как сообщение)|Меню, простые команды|
|**Инлайн** (`Inline`)|Под самим сообщением|Скрытый код (`callback_data`)|Ссылки, выборы, опросы|

---

## 2️⃣ Обычная клавиатура (Как пульт)

Кнопки нажимаются, и бот получает **текст** кнопки.

### 🛠 Создание:
```python
from telebot import types

# Создаём клавиатуру
markup = types.ReplyKeyboardMarkup(resize_keyboard=True)

# Создаём кнопки
btn1 = types.KeyboardButton('📞 Позвонить')
btn2 = types.KeyboardButton('📍 Адрес')

# Добавляем кнопки на клавиатуру
markup.add(btn1, btn2)

# Отправляем сообщение с клавиатурой
bot.send_message(chat_id, 'Выбери кнопку:', reply_markup=markup)
```

### 🎯 Обработка нажатия:

Бот думает, что это обычное текстовое сообщение.

```python
@bot.message_handler(content_types=['text'])
def handle_text(message):
    if message.text == '📞 Позвонить':
        bot.send_message(message.chat.id, '📱 Наш номер: 8-999-000-00-00')
    elif message.text == '📍 Адрес':
        bot.send_message(message.chat.id, '🏢 ул. Пушкина, д. 1')
```

> 💡 **Параметр `resize_keyboard=True`**: Делает клавиатуру компактной (не на пол-экрана).

---

## 3️⃣ Инлайн-клавиатура (Как ссылки)

Кнопки нажимаются, и бот получает **скрытый код**. Текст кнопки может не совпадать с кодом!

### 🛠 Создание:

```python
from telebot import types

markup = types.InlineKeyboardMarkup(row_width=2)

# callback_data — это метка, которую видит только бот
btn1 = types.InlineKeyboardButton('✅ Да', callback_data='yes')
btn2 = types.InlineKeyboardButton('❌ Нет', callback_data='no')
btn3 = types.InlineKeyboardButton('🌐 Сайт', url='https://google.com')

markup.add(btn1, btn2, btn3)

bot.send_message(chat_id, 'Ты согласен?', reply_markup=markup)
```

### 🎯 Обработка нажатия:

Для инлайн-кнопок нужен специальный обработчик `callback_query`.

```python
@bot.callback_query_handler(func=lambda call: True)
def callback_worker(call):
    # call — это нажатие, а не сообщение
    
    if call.data == 'yes':
        bot.answer_callback_query(call.id, 'Выбрано: Да!')
        bot.send_message(call.message.chat.id, 'Отлично!')
    elif call.data == 'no':
        bot.answer_callback_query(call.id, 'Выбрано: Нет!')
```

> ⚠️ **Важно:** Внутри `callback` используй `call.message.chat.id`, а не `message.chat.id`.  
> ⚠️ **Важно:** Всегда вызывай `bot.answer_callback_query(call.id)`, иначе телефон будет «крутить» значок загрузки.

---

## 4️⃣ Уровень ПРО: Функции для клавиатур 🧹

Чтобы не копировать код клавиатуры везде, создай для неё отдельную функцию.

```python
def get_main_menu():
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    markup.add('📞 Контакты', '📍 Адрес')
    return markup

# В коде бота:
@bot.message_handler(commands=['start'])
def start(message):
    bot.send_message(message.chat.id, 'Меню:', reply_markup=get_main_menu())
```

**Зачем это нужно?**

1. Код чище.
2. Если нужно добавить кнопку, меняешь в одном месте.
3. Легче читать.

---

## 5️⃣ Частые ошибки 🆘

|Ошибка|Почему?|Как исправить|
|---|---|---|
|Кнопки не нажимаются|Забыл `reply_markup`|Добавь `reply_markup=markup` в `send_message`.|
|Бот не видит нажатие|Путаница `text` и `data`|Обычные кнопки → `message.text`. Инлайн → `call.data`.|
|Ошибка `IndentationError`|Неправильные отступы|Проверь, чтобы код внутри `def` был сдвинут вправо.|
|`answer_callback_query` нет|Телефон «висит»|Всегда отвечай на нажатие инлайн-кнопки.|

---

## 📝 Домашнее задание

1. **Сделай меню:** Создай обычную клавиатуру с кнопками «О боте», «Помощь», «Контакты».
2. **Реакция:** Напиши код, чтобы бот отвечал разным текстом на каждую кнопку.
3. **Инлайн:** Создай сообщение с двумя инлайн-кнопками «👍» и «👎». Бот должен писать «Спасибо за оценку!» при нажатии.
4. **Чистота:** Вынеси создание клавиатуры в отдельную функцию `def get_menu():`.

---

> 💡 **Совет:** Для начала используй **Обычную клавиатуру** для главного меню. **Инлайн** оставь для выборов внутри сообщений (например, «Подтвердить заказ?»).

**Ты справишься! Кнопки делают бота живым! 🎹**